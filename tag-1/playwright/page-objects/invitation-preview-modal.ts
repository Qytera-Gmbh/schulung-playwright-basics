import { Locator, Page } from "@playwright/test";

export class InvitationPreviewModal {
  
  // Tipp
  // Für den Fall, dass sich Playwright wegen strict mode violations beschwert:
  // Die Selektion lässt sich sehr gut rein auf den Dialog einschränken, wenn man page.getByRole("dialog") verwendet

  // TODO

}
